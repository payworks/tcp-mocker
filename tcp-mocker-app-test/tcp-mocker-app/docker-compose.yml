version: "2"
services:
  tcp-mocker-app:
    image: tcpmocker/tcp-mocker-app:${PROJECT_VERSION}
    environment:
      - LOGGING_LEVEL_IO_PAYWORKS_LABS_TCPMOCKER=TRACE
    volumes:
      - ./tcp-mappings:/var/lib/tcp-mocker/tcp-mappings
