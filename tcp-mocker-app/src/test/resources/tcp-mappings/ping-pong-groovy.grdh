import static io.payworks.labs.tcpmocker.support.groovy.GroovyDataHandler.*

dataHandler()
        .request(binary()
                .stx()
                .ascii("ping")
                .etx())
        .response(binary()
                .stx()
                .ascii("pong")
                .etx())
        .build()